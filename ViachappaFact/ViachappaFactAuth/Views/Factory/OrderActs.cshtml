@model IEnumerable<ViachappaFactAuth.DB.OrderAct>

@{
    ViewBag.Title = "OrderActs";
}

<h2>OrderActs</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DocNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DocDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.isPost)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DocNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DocDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.isPost)
        </td>
        <td>
            
            @*@Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("OrderActDelete", "Delete", new { id=item.Id })*@
        </td>
    </tr>
}

</table>
